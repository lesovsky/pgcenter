---
# Creation date (YYYY-MM-DD)
created: 2026-08-03

# Status: draft | approved
status: approved

# Work type: feature | bug | refactoring
type: feature

# Feature size: S (1-3 files, local fix) | M (several components) | L (new architecture)
size: M
---

# User Spec: Пауза обновления экрана по `Space`

## Что делаем

Клавиша `Space` в `pgcenter top` замораживает изображение: сбор статистики продолжается как обычно,
но на экран новые кадры не попадают. Замерзает кадр целиком — таблица статистики, верхние панели
`sysstat`/`pgstat` и часы в шапке, — так что на экране остаётся согласованный снимок одного момента
вместе с отметкой, когда этот момент был. В командной строке появляется маркер `[PAUSED]`.

С замороженным кадром можно работать: фильтр, сброс фильтров, горизонтальный скролл колонок и
изменение их ширины продолжают действовать — это и отличает паузу от `tmux copy-mode`. Действия,
которым нужны свежие данные (сортировка, смена экрана, `,`, `I`, `A`, `v`, `B`/`N`/`F`/`L`), паузу
снимают; их перечень попадает во встроенную справку.

Новых экранов, колонок и SQL-запросов не появляется. Формат записи (`record`/`report`) не меняется.
Все изменения — внутри пакета `top/`.

Размер — **M**. Кода немного, но он весь в опасных местах: синхронизация между горутиной сбора и
горутиной интерфейса, хранение кадра, переживающее пересборку интерфейса, и перерисовка в
нескольких точках. Отсюда длинный список критериев приёмки при небольшом объёме правок: это не
широта функциональности, а плотность граничных случаев вокруг одной клавиши.

Основа — раздел `[016] Pause the display on Space` роадмапа 0.12.0. Фича отпочковалась от
[015-feat-tui-papercuts] 2026-08-02 и расширяет заложенный там композитор командной строки вторым
токеном.

## Зачем

Гарантия от внезапной смены кадра. На нагруженном кластере с интервалом обновления в секунду строка,
которую оператор читает, уезжает или исчезает ровно в момент чтения — и её приходится выцеплять
заново.

Два существующих обходных пути закрывают задачу лишь частично:

- **`tmux copy-mode`** замораживает экран, но отбирает управление: внутри него нельзя ни поставить
  фильтр, ни проскроллить колонки, ни развернуть verbose-панели. Плюс он требует tmux.
- **`z` с большим интервалом** (до 300 секунд) — это не пауза, а редкое обновление: кадр всё равно
  сменится в непредсказуемый момент, как правило именно тогда, когда его читают.

Честная рамка, унаследованная из роадмапа и подтверждённая при валидации: за фичей **не стоит
зафиксированный инцидент**, она обоснована аналогией с другими top-подобными инструментами плюс
разбором ограничений обходных путей выше. Это осознанная ставка, а не вывод из данных
(см. «Технические решения»).

## Пользовательские истории

- Как DBA, разбирающий инцидент, я хочу заморозить экран `activity` под нагрузкой, чтобы прочитать и
  скопировать строку долгой транзакции, которая иначе уедет при следующем обновлении.
- Как DBA, я хочу на замороженном кадре поставить фильтр и проскроллить колонки, чтобы рассмотреть
  подозрительный набор строк целиком, не потеряв тот самый момент.
- Как DBA, я хочу, чтобы выход из пейджера (`l` — просмотр лога) не сбрасывал паузу, чтобы вернуться
  ровно к тому кадру, ради которого я в лог и полез.

### Пользовательские сценарии

**Сценарий 1: заморозить строку под нагрузкой**

1. Оператор смотрит экран `activity` с интервалом обновления 1 секунда; строк много, они
   переупорядочиваются на каждом тике.
2. Замечает бэкенд с большим `xact_age` и нажимает `Space`.
3. Изображение немедленно перестаёт меняться; в командной строке появляется `[PAUSED]`; часы в шапке
   показывают момент заморозки.
4. Оператор спокойно читает и копирует строку.
5. Нажимает `Space` повторно — обновление возобновляется, первый же кадр обычный, без всплеска
   значений.

**Сценарий 2: разобрать замороженный кадр**

1. Оператор ставит паузу на экране `statements` в момент всплеска.
2. Нажимает `/`, вводит регулярное выражение — экран немедленно перерисовывается отфильтрованным,
   оставаясь замороженным; `[PAUSED]` на месте.
3. Скроллит колонки `[` / `]` и расширяет нужную колонку `↑` — каждое действие даёт видимый эффект на
   том же кадре.
4. Нажимает `\`, чтобы снять фильтры и снова увидеть весь кадр.
5. Нажимает `Space`, возвращаясь к живому обновлению.

**Сценарий 3: сходить в лог и вернуться**

1. Оператор ставит паузу, увидев подозрительную активность.
2. Нажимает `l` — открывается лог PostgreSQL в пейджере (интерфейс при этом пересобирается целиком).
3. Найдя подтверждение, выходит из пейджера.
4. На экране — тот же замороженный кадр, а не пустота и не свежие данные; `[PAUSED]` на месте.
5. Оператор сопоставляет запись лога со строкой на экране и снимает паузу.

**Сценарий 4: снятие паузы действием, которому нужны свежие данные**

1. Оператор на паузе решает пересортировать таблицу и нажимает `→`.
2. Пауза снимается, экран возвращается к живому обновлению уже с новым порядком сортировки;
   `[PAUSED]` исчезает.
3. Если оператор не понял, почему это произошло, он нажимает `h` и видит в справке строку о том,
   какие именно действия снимают паузу.

## Дизайн и интерфейс

### Страницы / экраны

- **Все экраны статистики:** пауза доступна везде, где действует основная раскладка клавиш —
  то есть когда на переднем плане сама таблица статистики, а не диалог, меню или справка.
  Отдельного экрана фича не добавляет.

### Ключевые компоненты

- **Клавиша `Space`:** переключатель паузы. Действует только когда активна таблица статистики,
  поэтому пробел, введённый в диалог (например, в регулярное выражение фильтра), остаётся обычным
  пробелом.
- **Маркер `[PAUSED]` в командной строке:** появляется немедленно при постановке паузы и держится
  всё время, пока она активна. Стоит левее индикатора активного фильтра `[F:...]`. После пересборки
  интерфейса (возврат из пейджера или редактора) маркер восстанавливается собственной записью в
  командную строку — не как побочный эффект перерисовки кадра, потому что кадра может и не быть.
- **Замороженный кадр:** таблица статистики, обе верхние панели и часы в шапке показывают одно и то
  же состояние — то, что было собрано на момент нажатия `Space`. Часы показывают время **того
  кадра, который на экране**, а не момент нажатия клавиши (эти значения расходятся не более чем на
  один интервал обновления). Если замороженным оказался самый первый кадр после запуска, его
  перерисовка не должна показывать подсказку сбора данных вместо самих данных — они в кадре уже
  есть.
- **Строка во встроенной справке (`h`):** описывает клавишу и перечисляет действия, снимающие паузу.

### Формы и ввод данных

Своих форм и полей ввода фича не добавляет. Единственное касание существующих диалогов — маркер
`[PAUSED]` занимает часть ширины строки, поэтому подсказка диалога на узком терминале обрезается
раньше обычного.

### UX-поведение

- **Постановка паузы:** изображение замирает мгновенно; никакого текстового сообщения не печатается
  — маркер `[PAUSED]` самодостаточен, а сообщение исчезло бы через две секунды и создало бы мигание.
- **Что работает на замороженном кадре** (пауза сохраняется): фильтр `/`, сброс фильтров `\`,
  скролл колонок `[` / `]`, ширина колонок `↑` / `↓`, изменение размера окна терминала, справка `h`,
  открытие и закрытие меню и диалогов, действия над БД (`-`, `_`, `k`, `K`, `R`), задание маски `n`
  и её показ `m`, смена интервала `z`, сброс счётчиков `Q`, а также походы во внешние программы, после которых
  интерфейс пересобирается: пейджер лога `l`, просмотр конфига `C`, `psql` `~`, отчёт по запросу
  `G` и редактор конфигов (`E` → выбор файла).
- **Что снимает паузу** (нужны свежие данные): сортировка (`←`, `→`, `<`), показ системных таблиц
  `,`, показ idle-соединений `I`, порог возраста `A`, смена экрана — как клавишей (`a`, `b`, `d`,
  `f`, `i`, `j`, `o`, `p`, `r`, `s`, `t`, `w`, `x`), так и **выбором пункта в меню** `D` / `X` /
  `P` / `J`, — переход на per-process статистику `S`, verbose-режим `v`, боковые панели `B`, `N`,
  `F`, `L`.
- **Меню — уточнение, чтобы не было двусмысленности:** само *открытие* меню и выход из него по
  `Esc` паузу не снимают (это лишь всплывающий слой поверх кадра). Паузу снимает *выбор пункта*,
  потому что он переключает экран. Исключение — меню конфигов `E`: выбор пункта открывает редактор,
  а это путь с пересборкой интерфейса, где пауза сохраняется.
- **Диалоги — то же правило:** открытие диалога и отмена по `Esc` паузу не снимают. Решает
  *применённое значение*: `A` (порог возраста) меняет запрос, поэтому снимает паузу; `/` (фильтр) и
  `\` действуют на отрисовке и паузу сохраняют; `z` (интервал), `n` (маска) и диалоги действий над
  БД паузу тоже сохраняют — их эффект виден после снятия.
- **Действие, которое ничего не изменило, паузу не снимает.** Часть обработчиков завершается
  досрочно, ничего не сделав: `x` на инсталляции без `pg_stat_statements`, `S` при подключении к
  удалённому серверу, `L` при недоступном файле лога. В таких случаях пауза сохраняется — снимать её
  незачем, новых данных запрошено не было.
- **Действия, эффект которых виден только после снятия паузы:** смена интервала `z`, сброс
  счётчиков `Q` и действия над БД (`-`, `_`, `k`, `K`, `R`). Отдельно про `z`: новое значение
  показывается в шапке, а шапка на паузе не перерисовывается — то есть подтверждения на экране не
  будет до снятия паузы. Это ожидаемое поведение, а не потерянное нажатие.
- **Приоритет маркера:** на узком терминале первым сжимается и выпадает индикатор фильтра, а
  `[PAUSED]` остаётся всегда. Осознанный компромисс: оба маркера вместе занимают около 18 колонок,
  так что конфликт возникает лишь на неразумно узком экране.
- **Известное следствие:** пока пауза активна, подсказка в диалогах становится примерно на 8 символов
  короче — на узком терминале длинная подсказка обрежется раньше обычного. Это не дефект.
- **Боковая панель логов**, открытая до постановки паузы, замирает вместе с кадром: пока пауза
  активна, файл лога не перечитывается и на экране остаются те строки, что были видны в момент
  нажатия `Space`. Иначе лог убегал бы вперёд поверх мёртвой статистики — экран перестал бы быть
  снимком одного момента, а каждая перерисовка ещё и рисковала бы уронить интерфейс ошибкой чтения
  файла. Показанные строки лога хранятся вместе с замороженным кадром, поэтому перерисовка — включая
  возврат из пейджера, где вся область создаётся заново, — восстанавливает панель, а не оставляет её
  пустой. Нажатие `L` во время паузы, как и остальных панелей, паузу снимает.
- **Ошибки во время паузы** (потеря соединения, сбой сбора статистики) на экран не пробиваются: они
  видны на первом же кадре после снятия паузы.

## Как должно работать

### Основной сценарий

1. Оператор нажимает `Space` на живом экране.
2. Кадр замирает целиком, в командной строке появляется `[PAUSED]`.
3. Сбор статистики продолжается в обычном темпе; приходящие кадры принимаются и отбрасываются.
   **Замороженным считается кадр, который был на экране в момент нажатия `Space`: пока пауза
   активна, сохранённый кадр не заменяется приходящими.** Иначе любая перерисовка нарисовала бы
   самые свежие данные, и заморозка была бы мнимой — при обновлении раз в секунду это заметить
   глазом почти невозможно. Обратная половина правила: пока пауза выключена, сохранённым становится
   каждый отрисованный кадр — то есть в момент нажатия `Space` замораживается ровно то, что оператор
   видит на экране.
4. Оператор работает с замороженным кадром (фильтр, скролл, ширина колонок) или уходит в
   пейджер и возвращается.
5. Оператор нажимает `Space` повторно (либо выполняет действие из списка снимающих) — обновление
   возобновляется, `[PAUSED]` исчезает.
6. Первый кадр после снятия паузы — обычный свежий кадр: значения за интервал, а не за всё время
   паузы.

### Граничные случаи

- **`Space` нажат до появления первого кадра** (сразу после старта): пауза включается, `[PAUSED]`
  виден, а экран остаётся пустым до снятия паузы — данных ещё нет. Сохранённого кадра тоже нет, и
  перерисовывать нечего, поэтому и после возврата из пейджера в таком состоянии на экране будет
  только `[PAUSED]`. Это допустимо: показывать нечего.
- **Изменение размера терминала во время паузы:** кадр перерисовывается под новую ширину, окно
  видимых колонок пересчитывается. Стрелки `‹` и `›` по краям таблицы (появились в
  [009-feat-horizontal-scroll]) показывают, что часть колонок скрыта слева или справа; после
  перерисовки они должны соответствовать новой ширине — пропадать, когда скрытых колонок в эту
  сторону не осталось.
- **Снятие паузы при большом интервале обновления:** маркер `[PAUSED]` исчезает сразу, а
  изображение сменится только со следующим кадром сборщика — при `z`, выставленном в сотни секунд,
  это заметная задержка. Данные на экране в этот момент честно те же, что и были; новых ещё нет.
- **Возврат из пейджера, редактора или psql во время паузы:** интерфейс пересобирается, после чего
  замороженный кадр рисуется заново — пустого экрана быть не должно.
- **Открытие и закрытие справки, меню или диалога во время паузы:** содержимое под ними
  восстанавливается само, дополнительной перерисовки не требуется.
- **Расширение колонки во время паузы:** значение показывается полностью, а не остаётся навсегда
  обрезанным с `~`.
- **Ошибка сбора статистики во время паузы:** кадр отбрасывается наравне с остальными, ошибка видна
  после снятия паузы.
- **Панель логов открыта на момент постановки паузы:** ошибка чтения файла лога во время паузы
  возникнуть не может — файл не читается вовсе (см. «UX-поведение»).
- **Возврат из пейджера при открытой панели логов:** восстанавливается и таблица статистики, и
  панель логов — обе из сохранённого кадра. Пустых областей на экране быть не должно — при условии,
  что кадр был сохранён (см. случай с паузой до первого кадра выше).
- **Пауза во время работы `pgcenter record` / `report` / `profile`:** неприменимо — фича существует
  только в интерактивном режиме `top`.

## Критерии приёмки

- [ ] `Space` на живом экране немедленно замораживает кадр целиком (таблица, обе верхние панели,
      часы в шапке), и `[PAUSED]` появляется в командной строке в тот же момент.
- [ ] Заморозка держится неограниченно долго, при этом сбор статистики ни разу не блокируется —
      проверяется тестом, в котором сборщик успешно завершает не менее пяти отправок подряд, пока
      активна пауза.
- [ ] Показанный на паузе кадр — тот, что был на экране в момент нажатия `Space`: после нескольких
      минут паузы перерисовка показывает исходные значения, а не текущие. Проверяется на экране с
      быстро меняющимися числами.
- [ ] Ни одно нажатие клавиши во время паузы не приводит к зависанию интерфейса — проверяется
      прогоном по всем клавишам основной раскладки на паузе (шаг 11 таблицы ниже).
- [ ] На замороженном кадре дают видимый эффект: фильтр `/` (с вводом выражения и `Enter`), сброс
      фильтров `\`, скролл `[` / `]`, изменение ширины `↑` / `↓`.
- [ ] Каждое из действий, снимающих паузу (сортировка `←`/`→`/`<`, `,`, `I`, `A`, смена экрана
      клавишей, выбор пункта в меню `D`/`X`/`P`/`J`, `S`, `v`, `B`/`N`/`F`/`L`), возвращает живое
      обновление, и первый кадр после этого — обычный, без всплеска значений.
- [ ] Открытие меню и выход из него по `Esc` паузу не снимают; выбор пункта — снимает (кроме меню
      конфигов `E`, где выбор ведёт в редактор и пауза сохраняется).
- [ ] Ошибка сбора статистики или потеря соединения во время паузы не меняют изображение; сообщение
      появляется на первом кадре после снятия паузы.
- [ ] Встроенная справка (`h`) открывается и закрывается во время паузы, не снимая её.
- [ ] Панель логов, открытая до паузы, не пополняется новыми строками, пока пауза активна, даже
      если в лог в это время идёт запись, — и восстанавливается со своими строками после возврата
      из пейджера, а не остаётся пустой.
- [ ] Встроенная справка (`h`) содержит строку про `Space` с перечнем действий, снимающих паузу.
- [ ] Выход из пейджера/редактора во время паузы показывает тот же замороженный кадр, а не пустой
      экран; `[PAUSED]` остаётся на месте. Единственное исключение — пауза была поставлена до
      появления первого кадра: показывать тогда нечего, и экран остаётся пустым с маркером.
- [ ] Изменение размера терминала во время паузы перерисовывает кадр под новую ширину: после
      расширения видно больше колонок, чем до него, после сужения — меньше, и стрелки `‹` / `›`
      исчезают с той стороны, где скрытых колонок не осталось.
- [ ] Повторный `Space` возвращает живое обновление; первый кадр после снятия — обычный свежий кадр.
- [ ] `Space`, нажатый до первого кадра, включает паузу: `[PAUSED]` появляется, экран остаётся
      пустым, и после снятия паузы приходит обычный первый кадр.
- [ ] Маркер `[PAUSED]` восстанавливается после возврата из пейджера или редактора — в том числе
      когда сохранённого кадра нет и перерисовывать нечего.
- [ ] Расширение колонки во время паузы не приводит к навсегда обрезанному значению.
- [ ] Во время паузы подсказка в диалогах короче примерно на 8 символов — на узком терминале она
      обрезается раньше обычного. Это ожидаемое поведение, а не дефект.
- [ ] На узком терминале с активным фильтром `[PAUSED]` остаётся виден, индикатор фильтра уступает
      место первым.
- [ ] Пробел, введённый в диалог (например, в регулярное выражение фильтра), паузу не включает.
- [ ] `make test`, `make lint`, `make vuln` проходят; гонок при `-race` нет.

## Ограничения

- Только интерактивный режим `top`; `record`, `report` и `profile` не затрагиваются.
- Никаких изменений в структуре `view.View`, SQL-запросах и формате записи — совместимость архивов
  сохраняется полностью.
- Поддерживаемые версии PostgreSQL не затрагиваются: ветвлений по версиям фича не добавляет.
- Флага командной строки и настроек у паузы нет — файла конфигурации в проекте не существует.
- Терминал: маркер занимает около 8 колонок, что на очень узком экране сокращает место под другие
  элементы командной строки (осознанный компромисс, см. «Дизайн и интерфейс»).

## Риски

- **Риск 1: зависание интерфейса.** Сборщик статистики отправляет кадры в небуферизованный канал и
  переходит к приёму команд от интерфейса только после успешной отправки. Пауза, которая просто
  перестала бы принимать кадры, привела бы к тому, что первое же нажатие клавиши навсегда подвесило
  бы интерфейс — включая сам `Space`, то есть выйти можно было бы только убив процесс.
  **Митигация:** пауза обязана принимать кадры и отбрасывать их, а не переставать их читать; это
  фиксируется тестом, который проверяет, что сборщик продолжает работать во время паузы.
- **Риск 2: клавиша молча ничего не делает.** Скролл, изменение ширины и сброс фильтров сегодня
  добиваются перерисовки, отправляя внеочередной кадр через сборщик; во время паузы такой кадр
  отбрасывается. **Митигация:** для каждого такого действия делается явная перерисовка из
  сохранённого кадра; все они проверяются на стенде поимённо. Сюда же входит применение фильтра
  через диалог — единственный путь, который не отправляет внеочередной кадр даже в обычном режиме.
- **Риск 3: бесконечная пересборка интерфейса.** Ошибка при перерисовке роняет цикл обработки
  событий, интерфейс пересобирается, снова вызывает перерисовку — и так по кругу. Источников
  ошибки два: обращение к ещё не созданному элементу интерфейса и чтение файла лога боковой
  панелью. **Митигация:** перерисовка выполняется в точке, где элементы интерфейса гарантированно
  уже созданы, и обоснование этого порядка фиксируется в техническом задании, а не подразумевается;
  панель логов на паузе файл не читает, что убирает второй источник целиком.
- **Риск 4: регресс отрисовки.** Ради корректной перерисовки замороженного кадра меняется поведение
  вывода ячейки таблицы — горячий путь, покрытый существующими тестами. **Митигация:** unit-тесты
  плюс прогон на стенде со вторым бинарником, собранным из `master`, чтобы отделить регресс от
  давно существующего поведения.
- **Риск 5: «залипшая» пауза.** Оператор забывает про режим и читает мёртвые числа как текущее
  состояние кластера. **Митигация:** `[PAUSED]` виден всегда и не уступает место никакому другому
  элементу командной строки.
- **Риск 6: перерисовка при изменении размера терминала затрагивает общий код раскладки.** Событие
  об изменении размера приложению не приходит — новый размер виден только функции раскладки, которая
  вызывается на каждом обновлении экрана. Значит детектор изменения размера придётся встроить именно
  туда, то есть в код, общий для всех экранов и режимов, а не в код паузы. **Митигация:** это
  единственное требование фичи, выходящее за её собственный периметр; механизм и доказательство его
  безопасности выносятся в техническое задание отдельным решением, а не проектируются по ходу
  реализации.
- **Риск 7: ставка без подтверждённого спроса.** За фичей нет зафиксированного инцидента; при
  валидации предлагалось отложить её или урезать до неинтерактивной. **Митигация:** решение
  владельца роадмапа принято осознанно и записано (см. «Технические решения»); объём ограничен
  пакетом `top/` и не создаёт долговременных обязательств — ни формата записи, ни настроек, ни
  публичного API.

## Технические решения

- Мы решили **не останавливать сбор статистики**, а гасить только отрисовку — иначе первый кадр
  после снятия паузы показал бы изменение за всё время паузы в колонках, размеченных как «за
  интервал» (десять минут паузы при обновлении раз в секунду дали бы число примерно в 600 раз
  больше обычного).
- Мы решили, что пауза **принимает и отбрасывает** кадры, а не перестаёт их читать — иначе
  интерфейс зависает намертво (Риск 1).
- Мы решили **заморозить и часы в шапке**, хотя сегодня время берётся в момент отрисовки: живые часы
  над мёртвыми данными — несогласованный кадр, а согласованность и есть смысл паузы.
- Мы решили, что **пауза переживает поход в пейджер и редактор**, и после пересборки интерфейса
  замороженный кадр рисуется заново. Альтернатива — снимать паузу при возврате — отвергнута: молча
  слетевший режим это ровно тот класс дефектов, ради которого в [015] делали индикатор фильтра.
- Мы решили **сохранить интерактивность замороженного кадра** (фильтр, скролл, ширина колонок),
  хотя именно она составляет большую часть стоимости фичи. Без неё пауза почти вырождается в
  `tmux copy-mode`, и главный аргумент «зачем это нужно» теряется.
- Мы решили, что **сохранённый кадр не обновляется, пока пауза активна**. Кадры продолжают
  приходить и отбрасываться, но заменять ими сохранённый нельзя: тогда перерисовка показывала бы
  свежие данные, заморозка была бы мнимой, и заметить это глазом при обновлении раз в секунду почти
  невозможно.
- Мы решили **заморозить и боковую панель логов**: пока пауза активна, файл лога не перечитывается.
  Иначе лог убегал бы вперёд поверх мёртвой статистики, а каждая перерисовка могла бы уронить
  интерфейс ошибкой чтения файла. Вместе с кадром сохраняются и показанные строки лога — иначе после
  возврата из пейджера панель оказалась бы пустой: её область создаётся заново, а единственный код,
  который её наполняет, — это как раз чтение файла, отключённое на паузе.
- Мы решили **сохранить перерисовку при изменении размера терминала**, хотя ради неё придётся
  встроить детектор изменения размера в общий код раскладки — единственное место, куда этот размер
  вообще приходит. Альтернатива (оставить кадр как есть до снятия паузы) отвергнута: расширив окно,
  оператор ожидает увидеть больше колонок.
- Мы решили **не пробивать ошибки сквозь паузу**. Сквозной проход дал бы ложную гарантию: часть
  ошибок сбора и без того теряется и приходит пустым кадром, а не кадром с ошибкой. Оператор
  осознанно смотрит на замороженный экран и увидит реальность одним нажатием позже.
- Мы решили, что маркер `[PAUSED]` **не сжимается никогда**, даже ценой индикатора фильтра: оба
  вместе занимают около 18 колонок, и терминал, на котором они не помещаются, за рабочий сценарий не
  считается.
- Мы решили, что постановка паузы **не печатает текстового сообщения** — ровно одна тихая
  перерисовка строки. Два сообщения в одном действии в этом коде приводят к тому, что видно
  остаётся только одно, а лишний текст всё равно исчез бы через две секунды.
- Мы решили сделать **вывод ячейки таблицы неразрушающим** вместо копирования кадра при сохранении:
  это чинит причину — сегодня отрисовка правит данные на месте, и для экранов без вычисления
  разницы (включая `activity`, экран по умолчанию) эта правка попадает в собственный снимок
  сборщика.
- Мы решили **не добавлять паузе флага командной строки и настройки** — файла конфигурации в проекте
  нет, а заводить его ради одного режима несоразмерно.
- Мы решили **оставить фичу в релизе 0.12.0 следующей по очереди**, хотя при валидации предлагалось
  отложить её (как [014]) или поставить после [017]–[020]: у паузы нет пересечения по коду с
  оставшимися фичами, и отсрочка ничего бы не стоила, но порядок роадмапа сохранён решением
  владельца. Более дешёвые варианты — неинтерактивная пауза и пауза, не переживающая пейджер — были
  рассмотрены и отвергнуты явно.

## Тестирование

**Unit-тесты:** делаются всегда, не обсуждаются. Ключевые: сборщик статистики продолжает работать во
время паузы (проверка на подставных каналах), маркер `[PAUSED]` в командной строке, неразрушающий
вывод ячейки таблицы, отрисовка с сохранённой отметкой времени.

**Интеграционные тесты:** не делаем — фича не добавляет ни одного обращения к БД и не меняет ни
одного запроса.

**E2E тесты:** не делаем — автоматизированного end-to-end контура для TUI в проекте нет; его роль
выполняет ручной прогон на стенде (см. ниже).

## Как проверить

Ручные проверки идут по регламенту работы со стендом из `patterns.md` («Driving the TUI on a remote
test stand»): свежий `make build` в начале сессии, запуск бинарника по явному пути, фиксированная
геометрия tmux, снятие экрана с сохранением escape-последовательностей там, где проверяются
атрибуты, и второй бинарник, собранный из `master`, для отделения регресса от давнего поведения.
Адрес стенда в репозитории не хранится — он запрашивается у владельца проекта перед каждым прогоном.

### Агент проверяет

| Шаг | Инструмент | Ожидаемый результат |
|-----|-----------|-------------------|
| 1. Прогон тестов и статического анализа | `make test`, `make lint`, `make vuln` | Зелено, гонок при `-race` нет |
| 2. Пауза на нагруженном экране `activity` (обновление 1 с) | tmux на стенде, `capture-pane` с интервалом | Два снимка с разницей ≥60 с идентичны; `[PAUSED]` виден; часы в шапке не изменились |
| 3. Работа с замороженным кадром | tmux `send-keys`: `/` + выражение + `Enter`, затем `\`, `[`, `]`, `↑`, `↓` | Каждое действие даёт видимое изменение кадра, `[PAUSED]` на месте; после фильтра строк на экране меньше |
| 3a. Кадр не подменяется свежим | пауза ≥3 мин на экране с быстро меняющимися числами, затем `[` и `capture-pane` | Значения те же, что были при постановке паузы |
| 4. Уход в пейджер и возврат | `send-keys l`, выход, `capture-pane` | Тот же кадр, экран не пустой, `[PAUSED]` на месте |
| 5. Изменение размера терминала во время паузы | `tmux resize-window` 190 → 60 → 190 | После сужения колонок на экране меньше, после расширения — больше; стрелка `›` пропадает, когда справа скрытых колонок не осталось |
| 6. Узкий терминал с активным фильтром | сессия `-x 60`, `capture-pane` | `[PAUSED]` виден, индикатор фильтра уступает место |
| 7. Снимающие клавиши — все | `send-keys` по очереди, с восстановлением паузы между шагами: `←`, `→`, `<`, `,`, `I`, `A`, `t`, `D`+`Enter`, `X`+`Enter`, `P`+`Enter`, `J`+`Enter`, `S`, `v`, `B`, `N`, `F`, `L` | Каждая снимает паузу, первый кадр обычный, без всплеска значений. Исключения по обстановке стенда: `S` при удалённом подключении и `L` при недоступном файле лога завершаются досрочно и паузу не снимают — это ожидаемо, а не дефект |
| 7a. Меню и диалог не снимают паузу открытием | `send-keys D`, затем `Esc`; затем `A`, затем `Esc` | `[PAUSED]` на месте, кадр не изменился |
| 7b. Ошибка во время паузы | остановить кластер на паузе, подождать, затем `Space` | Пока пауза активна, изображение не меняется; после снятия появляется сообщение об ошибке |
| 7c. Справка и пробел в диалоге | `send-keys h`, `Esc`; затем `/`, пробел в поле ввода, `Esc` | Справка открывается и закрывается, паузу не снимая; пробел в диалоге вводится как символ и паузу не переключает |
| 7d. Панель логов на паузе | открыть `L`, поставить паузу, писать в лог (`SELECT pg_reload_conf()` или заведомо ошибочный запрос), `capture-pane`; затем `l` (пейджер), выход, снова `capture-pane` | Новых строк в панели не появляется, пока пауза активна; после возврата из пейджера панель показывает те же строки, а не пустоту |
| 8. `Space` до первого кадра | запуск и немедленный `send-keys Space`, затем `l` и выход из пейджера | `[PAUSED]` появляется, экран пуст; после возврата из пейджера маркер на месте |
| 9. Расширение колонки во время паузы | `send-keys ↑` на замороженном кадре | Значение показано полностью, без остаточного `~` |
| 10. Сравнение с `master` | второй бинарник из `master`, те же шаги 2–9 | Различия объясняются фичей, а не давно существующим поведением |
| 11. Прогон по всей раскладке на паузе | `send-keys` каждой клавиши основной раскладки по очереди, между нажатиями `capture-pane` | Интерфейс отвечает на каждое нажатие, ни одно не приводит к зависанию |

### Пользователь проверяет

- Реальный кластер под нагрузкой: пауза действительно удерживает читаемую строку, а маркер
  невозможно не заметить — то есть фича решает исходную задачу, а не только проходит проверки.
- Субъективная оценка перечня снимающих действий: не оказывается ли какое-то из них неожиданным в
  живой работе.

## Post-implementation

Updated: 2026-08-04

### Divergences from original spec

- **Перечень снимающих действий:** спека перечисляет клавиши без оговорок → снятие паузы происходит
  только когда действие **действительно выполнилось**. Обработчик, ушедший в ранний возврат, паузу не
  снимает: `,` на экране без системных таблиц, `x`/`X` без `pg_stat_statements`, `S` на удалённом
  подключении, `L` при недоступном логе. Причина: Decision 9 ставит снятие ниже ранних возвратов,
  иначе действие без эффекта размораживало бы экран. На стенде подтверждено, что такие случаи
  сопровождаются сообщением в командной строке (`NOTICE: pg_stat_statements not found`), то есть
  молчаливого несрабатывания нет.

- **Командная строка при изменении размера терминала:** спека этого случая не описывала → кадр
  перерисовывается под новую ширину, а строка cmdline сохраняет прежний текст до следующей записи в
  неё и на суженном терминале выглядит обрезанной посреди токена. Лестница токенов при этом исправна
  — после любого события, пишущего в cmdline, она отрабатывает до `[PAUSED]` включительно (проверено
  до 10 колонок). Зарегистрировано как техдолг [033].

### Added during implementation

- `selectLogtail` — вынесенный выбор источника logtail (файл против сохранённого буфера). В спеке и
  тех-спеке этого шва не было; добавлен во втором раунде ревью, потому что свойство «перерисовка не
  читает файл» иначе не закреплялось тестом: два ревьюера независимо показали мутацией, что при
  удалённой проверке ветвления весь набор тестов оставался зелёным.

### Descoped / Deferred

- Ничего из заявленного в спеке не отброшено: все 21 критерий приёмки закрыты, из них 15 — ручным
  прогоном на стенде 2026-08-04.
- Экраны `pg_stat_statements` (`x`, `X`) на стенде не проверялись — расширение там не установлено.
  Механизм раннего возврата подтверждён через его сообщение; сам переход на эти экраны под паузой
  остаётся непроверенным на живом терминале.
